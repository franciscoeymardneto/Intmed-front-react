import { LoadingContainer, Spinner } from './styles';

const LoadingComponent = () => {
  return (
    <LoadingContainer>
      <Spinner />
    </LoadingContainer>
  );
};

export default LoadingComponent;
